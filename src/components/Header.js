import React from "react";
import styled, { keyframes } from "styled-components";

function Header(){
  const
  return(
    <div>
      <style jsx>{`
        .rows{
          display: flex;
          flex-direction: row;
          color: red;
          align-items: center;
        }
        `}
        
    </div>
  )
};

export default Header;
